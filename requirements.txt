# Money Graph — pinned for reproducibility (guideline.md §4).
# Core pipeline — no cloud, no GPU, no paid service required.
pandas==3.0.6
pyarrow==25.0.1
numpy==2.5.3
networkx==3.7
PyYAML==6.0.3

# Viewer
gradio==5.49.1
pyvis==0.3.2
plotly==6.3.1

# Agent layer — optional at runtime. Without OPENAI_KEY every agent falls back
# to its deterministic path, so the pipeline still produces all three exports.
openai==2.6.1
python-dotenv==1.1.1

# Tests
pytest==9.1.1
